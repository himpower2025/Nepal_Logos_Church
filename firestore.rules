rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 로그인한 모든 사용자가 데이터베이스의 모든 문서를 읽고 쓸 수 있도록 허용합니다.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}